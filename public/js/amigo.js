// Funcion para ocultar y mostrar el menu lateral
function mostrarOcultarMenu() {
  let icono = document.getElementById('iconoMostrarOcultarMenu');

  // Debemos ocultar menu
  if (icono.classList.contains('bi-caret-left-square-fill')) {
  }

  // Debemos mostrar el menu
  if (icono.classList.contains('bi-caret-right-square-fill')) {
  }
}
